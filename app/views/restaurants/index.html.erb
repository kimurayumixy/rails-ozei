<%= render "shared/navbar" %>
<div class="container">
  <div class="info-search-text">
    <% if @group_size == nil %>
      <small> <%= "All restaurants" %></small>
    <% else %>
      <small><%=  "Search #{@group_size} people" %></small>
    <% end %>
  </div>
</div>
<br>
<%= render 'map_modal' %>
<%= render 'request_icon' %>
<br>
<%= render 'list' %>
